part of 'run.dart';

/// Represents a final key value pair after resolving prompt.
class Input {
  Input({
    required this.key,
    required this.value,
  });

  final String key;
  final String value;
}
